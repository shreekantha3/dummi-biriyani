import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // UNCOMMENT below for GitHub Pages static export:
  // output: "export",
  // images: { unoptimized: true },
  // trailingSlash: true,
};

export default nextConfig;
